name: workflow1
on: push

jobs:
    firstjob:
        runs-on: ubuntu-latest    
        steps:
         - name: echo
           run: echo "hello world"
    secondjob:
        runs-on: ubuntu-latest
        steps:
         - name: echo hi
           run: |
                echo "hi m second job"
                echo next is third job
         - name: step2job2
           run: echo "last job"